DROP TABLE ACCOUNTS;
CREATE TABLE ACCOUNTS(
    ID INTEGER NOT NULL PRIMARY KEY,
    COMPLETE_NAME VARCHAR2(500) NOT NULL,
    EMAIL VARCHAR2(500) NOT NULL UNIQUE,
    PASSWORD VARCHAR2(64) NOT NULL,
    BIRTHDATE VARCHAR2(11) NOT NULL,
    -- 1 para moderador | 0 para usuário comum
    ROLE NUMBER(1) NOT NULL,
    TOKEN VARCHAR2(32) NOT NULL UNIQUE,
    CREATED_AT DATE DEFAULT SYSDATE
);
SELECT * FROM ACCOUNTS
ORDER BY COMPLETE_NAME;

SELECT * FROM WALLETS
ORDER BY FK_ID_USER;

UPDATE WALLETS
SET BALANCE = 999.99
WHERE FK_ID_USER = 422;
COMMIT;

INSERT INTO ACCOUNTS (ID, COMPLETE_NAME, EMAIL, PASSWORD, BIRTHDATE, ROLE, TOKEN)
VALUES( 
    SEQ_ACCOUNTS.NEXTVAL, 
    'Enzo Garofalo', 
    'enzo@puc', 
    '123123',
    '26/08/2003', 
    1, 
    DBMS_RANDOM.STRING('X', 32)
);

INSERT INTO ACCOUNTS (ID, COMPLETE_NAME, EMAIL, PASSWORD, BIRTHDATE, ROLE, TOKEN)
VALUES( 
    SEQ_ACCOUNTS.NEXTVAL, 
    'Rogério Medina', 
    'rogerio@puc', 
    '123123', 
    '26/08/1992', 
    1, 
    DBMS_RANDOM.STRING('X', 32)
);

CREATE SEQUENCE SEQ_ACCOUNTS START WITH 1 INCREMENT BY 1;
COMMIT;